;; -*- mode: emacs-lisp -*-
;; History entries used for anything adaptive display.
(setq anything-c-adaptive-history (quote ((command ("anything-resume" ("anything-resu" . 1)) ("anything-grep" ("anything-grep" . 1)) ("install-elisp-from-emacswiki" ("install-" . 1))))))
